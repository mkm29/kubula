# This is a template for dev environment secrets
# Encrypt this file using: sops -e -i secrets/dev/common.yaml
# Decrypt for viewing: sops -d secrets/dev/common.yaml

database:
  host: dev-db.example.com
  port: 5432
  username: dev_user
  password: REPLACE_WITH_ACTUAL_PASSWORD

bastion:
  ssh_key_name: dev-bastion-key
  allowed_cidrs:
    - 10.0.0.0/8  # Corporate network

eks:
  cluster_endpoint_private_access_cidrs:
    - 10.0.0.0/8  # Corporate network
  
monitoring:
  datadog_api_key: REPLACE_WITH_ACTUAL_KEY
  slack_webhook_url: REPLACE_WITH_ACTUAL_URL